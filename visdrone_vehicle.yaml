# Generated by scripts/prepare_visdrone.py (edit `path` to your machine).
# Target = vehicles: VisDrone categories 3 bicycle, 4 car, 5 van, 6 truck,
# 9 bus, 10 motor, 7 tricycle, 8 awning-tricycle  ->  single class 0 "vehicle".
# (Remove bicycle (3) from DEFAULT_MAP for motorised-only.)
#
# To target PERSONS instead, edit DEFAULT_MAP in prepare_visdrone.py to
# {1:0, 2:0} (pedestrian/people) and set names below to `person`.

path: /data/visdrone_yolo
train: images/train
val: images/val
nc: 1
names:
  0: vehicle
